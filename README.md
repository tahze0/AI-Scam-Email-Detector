# AI-Scam-Email-Detector

Install Dependencies:
pip install -r requirements.txt

Train the AI Models (Run once):
python scam_detector.py

Start the Server:
python -m uvicorn api:app --reload


Frontend Setup (React):
cd frontend

Install Node Packages (Run once):
npm install

Start the Website:
npm run dev

The website is running when you see a link like: http://localhost:5173
Open this link in your browser.